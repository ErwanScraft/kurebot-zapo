import type { WaStoredThreadRecord, WaThreadStore } from 'zapo-js/store';
import { BaseMysqlStore } from './BaseMysqlStore';
import type { WaMysqlStorageOptions } from './types';
export declare class WaThreadMysqlStore extends BaseMysqlStore implements WaThreadStore {
    constructor(options: WaMysqlStorageOptions);
    upsert(record: WaStoredThreadRecord): Promise<void>;
    upsertBatch(records: readonly WaStoredThreadRecord[]): Promise<void>;
    getByJid(jid: string): Promise<WaStoredThreadRecord | null>;
    list(limit?: number): Promise<readonly WaStoredThreadRecord[]>;
    deleteByJid(jid: string): Promise<number>;
    clear(): Promise<void>;
}
