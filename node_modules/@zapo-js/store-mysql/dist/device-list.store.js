"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.WaDeviceListMysqlStore = void 0;
const util_1 = require("zapo-js/util");
const BaseMysqlStore_1 = require("./BaseMysqlStore");
const helpers_1 = require("./helpers");
const DEFAULT_DEVICE_LIST_TTL_MS = 5 * 60 * 1000;
const BATCH_SIZE = 500;
const FIXED_IN_PLACEHOLDERS = Array.from({ length: BATCH_SIZE }, () => '?').join(', ');
class WaDeviceListMysqlStore extends BaseMysqlStore_1.BaseMysqlStore {
    constructor(options, ttlMs = DEFAULT_DEVICE_LIST_TTL_MS) {
        super(options, ['deviceList']);
        if (!Number.isFinite(ttlMs) || ttlMs <= 0) {
            throw new Error('device-list ttlMs must be a positive finite number');
        }
        this.ttlMs = ttlMs;
    }
    async upsertUserDevicesBatch(snapshots) {
        if (snapshots.length === 0)
            return;
        await this.withTransaction(async (conn) => {
            for (const snapshot of snapshots) {
                await conn.execute(`INSERT INTO ${this.t('device_list_cache')} (
                        session_id, user_jid, alt_user_jid, device_jids_json, updated_at_ms, expires_at_ms
                    ) VALUES (?, ?, ?, ?, ?, ?)
                    ON DUPLICATE KEY UPDATE
                        alt_user_jid = VALUES(alt_user_jid),
                        device_jids_json = VALUES(device_jids_json),
                        updated_at_ms = VALUES(updated_at_ms),
                        expires_at_ms = VALUES(expires_at_ms)`, [
                    this.sessionId,
                    snapshot.userJid,
                    snapshot.altUserJid ?? null,
                    JSON.stringify(snapshot.deviceJids),
                    snapshot.updatedAtMs,
                    snapshot.updatedAtMs + this.ttlMs
                ]);
            }
        });
    }
    async getUserDevicesBatch(userJids, nowMs = Date.now()) {
        if (userJids.length === 0)
            return [];
        await this.ensureReady();
        const uniqueUserJids = [...new Set(userJids)];
        const activeByUserJid = new Map();
        const expiredUserJids = [];
        for (let start = 0; start < uniqueUserJids.length; start += BATCH_SIZE) {
            const batch = uniqueUserJids.slice(start, start + BATCH_SIZE);
            while (batch.length < BATCH_SIZE)
                batch.push('');
            const rows = (0, helpers_1.queryRows)(await this.pool.execute(`SELECT user_jid, alt_user_jid, device_jids_json, updated_at_ms, expires_at_ms
                     FROM ${this.t('device_list_cache')}
                     WHERE session_id = ? AND user_jid IN (${FIXED_IN_PLACEHOLDERS})`, [this.sessionId, ...batch]));
            for (const row of rows) {
                const userJid = String(row.user_jid);
                const expiresAtMs = Number(row.expires_at_ms);
                if (expiresAtMs <= nowMs) {
                    expiredUserJids.push(userJid);
                    continue;
                }
                activeByUserJid.set(userJid, decodeMysqlSnapshot(userJid, row));
            }
        }
        if (expiredUserJids.length > 0) {
            for (let start = 0; start < expiredUserJids.length; start += BATCH_SIZE) {
                const batch = expiredUserJids.slice(start, start + BATCH_SIZE);
                while (batch.length < BATCH_SIZE)
                    batch.push('');
                await this.pool.execute(`DELETE FROM ${this.t('device_list_cache')}
                     WHERE session_id = ? AND expires_at_ms <= ? AND user_jid IN (${FIXED_IN_PLACEHOLDERS})`, [this.sessionId, nowMs, ...batch]);
            }
        }
        return userJids.map((jid) => activeByUserJid.get(jid) ?? null);
    }
    async findByAnyUserJid(jid, nowMs = Date.now()) {
        await this.ensureReady();
        const rows = (0, helpers_1.queryRows)(await this.pool.execute(`SELECT user_jid, alt_user_jid, device_jids_json, updated_at_ms, expires_at_ms
                 FROM ${this.t('device_list_cache')}
                 WHERE session_id = ? AND (user_jid = ? OR alt_user_jid = ?)
                 LIMIT 1`, [this.sessionId, jid, jid]));
        if (rows.length === 0)
            return null;
        const row = rows[0];
        const expiresAtMs = Number(row.expires_at_ms);
        if (expiresAtMs <= nowMs) {
            const expiredUserJid = String(row.user_jid);
            await this.pool.execute(`DELETE FROM ${this.t('device_list_cache')} WHERE session_id = ? AND user_jid = ?`, [this.sessionId, expiredUserJid]);
            return null;
        }
        return decodeMysqlSnapshot(String(row.user_jid), row);
    }
    async deleteUserDevices(userJid) {
        await this.ensureReady();
        return (0, helpers_1.affectedRows)(await this.pool.execute(`DELETE FROM ${this.t('device_list_cache')}
             WHERE session_id = ? AND user_jid = ?`, [this.sessionId, userJid]));
    }
    async cleanupExpired(nowMs) {
        await this.ensureReady();
        return (0, helpers_1.affectedRows)(await this.pool.execute(`DELETE FROM ${this.t('device_list_cache')}
             WHERE session_id = ? AND expires_at_ms <= ?`, [this.sessionId, nowMs]));
    }
    async clear() {
        await this.ensureReady();
        await this.pool.execute(`DELETE FROM ${this.t('device_list_cache')} WHERE session_id = ?`, [
            this.sessionId
        ]);
    }
}
exports.WaDeviceListMysqlStore = WaDeviceListMysqlStore;
function decodeMysqlSnapshot(userJid, row) {
    const rawJson = row.device_jids_json instanceof Uint8Array
        ? util_1.TEXT_DECODER.decode(row.device_jids_json)
        : String(row.device_jids_json);
    const parsed = JSON.parse(rawJson);
    if (!Array.isArray(parsed)) {
        throw new Error('device_list_cache.device_jids_json must be an array');
    }
    const altUserJid = row.alt_user_jid === null || row.alt_user_jid === undefined
        ? undefined
        : String(row.alt_user_jid);
    return {
        userJid,
        ...(altUserJid !== undefined ? { altUserJid } : {}),
        deviceJids: parsed.map((entry) => String(entry)),
        updatedAtMs: Number(row.updated_at_ms)
    };
}
