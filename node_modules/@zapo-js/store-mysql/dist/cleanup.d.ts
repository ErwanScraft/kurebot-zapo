import type { WaChatMetadataStore, WaDeviceListStore, WaGroupMetadataStore, WaMessageSecretStore, WaRetryStore } from 'zapo-js/store';
export interface MysqlCleanupPollerOptions {
    readonly intervalMs?: number;
    readonly retry?: WaRetryStore;
    readonly groupMetadata?: WaGroupMetadataStore;
    readonly chatMetadata?: WaChatMetadataStore;
    readonly deviceList?: WaDeviceListStore;
    readonly messageSecret?: WaMessageSecretStore;
    readonly onError?: (error: Error) => void;
}
export declare class MysqlCleanupPoller {
    private readonly intervalMs;
    private readonly retry;
    private readonly groupMetadata;
    private readonly chatMetadata;
    private readonly deviceList;
    private readonly messageSecret;
    private readonly onError;
    private timer;
    private inFlight;
    constructor(options: MysqlCleanupPollerOptions);
    start(): void;
    stop(): void;
    cleanup(): Promise<number>;
}
