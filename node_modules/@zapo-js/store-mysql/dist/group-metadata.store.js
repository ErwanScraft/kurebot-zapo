"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.WaGroupMetadataMysqlStore = void 0;
const BaseMysqlStore_1 = require("./BaseMysqlStore");
const helpers_1 = require("./helpers");
const DEFAULT_GROUP_METADATA_TTL_MS = 5 * 60 * 1000;
class WaGroupMetadataMysqlStore extends BaseMysqlStore_1.BaseMysqlStore {
    constructor(options, ttlMs = DEFAULT_GROUP_METADATA_TTL_MS) {
        super(options, ['participants']);
        if (!Number.isFinite(ttlMs) || ttlMs <= 0) {
            throw new Error('groupMetadata ttlMs must be a positive finite number');
        }
        this.ttlMs = ttlMs;
    }
    async upsertGroupMetadata(snapshot) {
        await this.ensureReady();
        await this.pool.execute(`INSERT INTO ${this.t('group_participants_cache')} (
                session_id, group_jid, participants_json, ephemeral, ephemeral_trigger, updated_at_ms, expires_at_ms
            ) VALUES (?, ?, ?, ?, ?, ?, ?)
            ON DUPLICATE KEY UPDATE
                participants_json = VALUES(participants_json),
                ephemeral = VALUES(ephemeral),
                ephemeral_trigger = VALUES(ephemeral_trigger),
                updated_at_ms = VALUES(updated_at_ms),
                expires_at_ms = VALUES(expires_at_ms)`, [
            this.sessionId,
            snapshot.groupJid,
            JSON.stringify(snapshot.participants),
            snapshot.ephemeral ?? null,
            snapshot.ephemeralTrigger ?? null,
            snapshot.updatedAtMs,
            snapshot.updatedAtMs + this.ttlMs
        ]);
    }
    async getGroupMetadata(groupJid, nowMs = Date.now()) {
        await this.ensureReady();
        const row = (0, helpers_1.queryFirst)(await this.pool.execute(`SELECT group_jid, participants_json, ephemeral, ephemeral_trigger, updated_at_ms, expires_at_ms
             FROM ${this.t('group_participants_cache')}
             WHERE session_id = ? AND group_jid = ?`, [this.sessionId, groupJid]));
        if (!row)
            return null;
        if (Number(row.expires_at_ms) <= nowMs) {
            await this.pool.execute(`DELETE FROM ${this.t('group_participants_cache')}
                 WHERE session_id = ? AND group_jid = ? AND expires_at_ms <= ?`, [this.sessionId, groupJid, nowMs]);
            return null;
        }
        const rawJson = row.participants_json instanceof Uint8Array
            ? new TextDecoder().decode(row.participants_json)
            : String(row.participants_json);
        const parsed = JSON.parse(rawJson);
        if (!Array.isArray(parsed)) {
            throw new Error('group_participants_cache.participants_json must be an array');
        }
        const ephemeral = row.ephemeral === null ? undefined : Number(row.ephemeral);
        const ephemeralTrigger = row.ephemeral_trigger === null || row.ephemeral_trigger === undefined
            ? undefined
            : Number(row.ephemeral_trigger);
        return {
            groupJid: String(row.group_jid),
            participants: parsed.map((entry) => String(entry)),
            ephemeral,
            ephemeralTrigger,
            updatedAtMs: Number(row.updated_at_ms)
        };
    }
    async deleteGroupMetadata(groupJid) {
        await this.ensureReady();
        return (0, helpers_1.affectedRows)(await this.pool.execute(`DELETE FROM ${this.t('group_participants_cache')}
             WHERE session_id = ? AND group_jid = ?`, [this.sessionId, groupJid]));
    }
    async cleanupExpired(nowMs) {
        await this.ensureReady();
        return (0, helpers_1.affectedRows)(await this.pool.execute(`DELETE FROM ${this.t('group_participants_cache')}
             WHERE session_id = ? AND expires_at_ms <= ?`, [this.sessionId, nowMs]));
    }
    async clear() {
        await this.ensureReady();
        await this.pool.execute(`DELETE FROM ${this.t('group_participants_cache')} WHERE session_id = ?`, [this.sessionId]);
    }
}
exports.WaGroupMetadataMysqlStore = WaGroupMetadataMysqlStore;
