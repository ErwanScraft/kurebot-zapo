"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.WaMessageSecretMysqlStore = void 0;
const BaseMysqlStore_1 = require("./BaseMysqlStore");
const helpers_1 = require("./helpers");
const DEFAULT_MESSAGE_SECRET_TTL_MS = 30 * 60 * 1000;
const BATCH_SIZE = 500;
const FIXED_IN_PLACEHOLDERS = Array.from({ length: BATCH_SIZE }, () => '?').join(', ');
class WaMessageSecretMysqlStore extends BaseMysqlStore_1.BaseMysqlStore {
    constructor(options, ttlMs = DEFAULT_MESSAGE_SECRET_TTL_MS) {
        super(options, ['messageSecret']);
        if (!Number.isFinite(ttlMs) || ttlMs <= 0) {
            throw new Error('message-secret ttlMs must be a positive finite number');
        }
        this.ttlMs = ttlMs;
    }
    async get(messageId, nowMs = Date.now()) {
        await this.ensureReady();
        const rows = (0, helpers_1.queryRows)(await this.pool.execute(`SELECT secret, sender_jid, expires_at_ms
                 FROM ${this.t('message_secrets_cache')}
                 WHERE session_id = ? AND message_id = ?`, [this.sessionId, messageId]));
        if (rows.length === 0)
            return null;
        const row = rows[0];
        if (Number(row.expires_at_ms) <= nowMs) {
            await this.pool.execute(`DELETE FROM ${this.t('message_secrets_cache')}
                 WHERE session_id = ? AND message_id = ? AND expires_at_ms <= ?`, [this.sessionId, messageId, nowMs]);
            return null;
        }
        return { secret: (0, helpers_1.toBytes)(row.secret), senderJid: String(row.sender_jid) };
    }
    async getBatch(messageIds, nowMs = Date.now()) {
        if (messageIds.length === 0)
            return [];
        await this.ensureReady();
        const uniqueIds = [...new Set(messageIds)];
        const activeById = new Map();
        const expiredIds = [];
        for (let start = 0; start < uniqueIds.length; start += BATCH_SIZE) {
            const batch = uniqueIds.slice(start, start + BATCH_SIZE);
            while (batch.length < BATCH_SIZE)
                batch.push('');
            const rows = (0, helpers_1.queryRows)(await this.pool.execute(`SELECT message_id, secret, sender_jid, expires_at_ms
                     FROM ${this.t('message_secrets_cache')}
                     WHERE session_id = ? AND message_id IN (${FIXED_IN_PLACEHOLDERS})`, [this.sessionId, ...batch]));
            for (const row of rows) {
                const id = String(row.message_id);
                const expiresAtMs = Number(row.expires_at_ms);
                if (expiresAtMs <= nowMs) {
                    expiredIds.push(id);
                    continue;
                }
                activeById.set(id, {
                    secret: (0, helpers_1.toBytes)(row.secret),
                    senderJid: String(row.sender_jid)
                });
            }
        }
        if (expiredIds.length > 0) {
            for (let start = 0; start < expiredIds.length; start += BATCH_SIZE) {
                const batch = expiredIds.slice(start, start + BATCH_SIZE);
                while (batch.length < BATCH_SIZE)
                    batch.push('');
                await this.pool.execute(`DELETE FROM ${this.t('message_secrets_cache')}
                     WHERE session_id = ? AND expires_at_ms <= ? AND message_id IN (${FIXED_IN_PLACEHOLDERS})`, [this.sessionId, nowMs, ...batch]);
            }
        }
        return messageIds.map((id) => activeById.get(id) ?? null);
    }
    async set(messageId, entry) {
        await this.ensureReady();
        const nowMs = Date.now();
        await this.pool.execute(`INSERT INTO ${this.t('message_secrets_cache')} (
                session_id, message_id, secret, sender_jid, expires_at_ms
            ) VALUES (?, ?, ?, ?, ?)
            ON DUPLICATE KEY UPDATE
                secret = VALUES(secret),
                sender_jid = VALUES(sender_jid),
                expires_at_ms = VALUES(expires_at_ms)`, [this.sessionId, messageId, entry.secret, entry.senderJid, nowMs + this.ttlMs]);
    }
    async setBatch(entries) {
        if (entries.length === 0)
            return;
        const nowMs = Date.now();
        await this.withTransaction(async (conn) => {
            for (const e of entries) {
                await conn.execute(`INSERT INTO ${this.t('message_secrets_cache')} (
                        session_id, message_id, secret, sender_jid, expires_at_ms
                    ) VALUES (?, ?, ?, ?, ?)
                    ON DUPLICATE KEY UPDATE
                        secret = VALUES(secret),
                        sender_jid = VALUES(sender_jid),
                        expires_at_ms = VALUES(expires_at_ms)`, [
                    this.sessionId,
                    e.messageId,
                    e.entry.secret,
                    e.entry.senderJid,
                    nowMs + this.ttlMs
                ]);
            }
        });
    }
    async cleanupExpired(nowMs) {
        await this.ensureReady();
        return (0, helpers_1.affectedRows)(await this.pool.execute(`DELETE FROM ${this.t('message_secrets_cache')}
             WHERE session_id = ? AND expires_at_ms <= ?`, [this.sessionId, nowMs]));
    }
    async clear() {
        await this.ensureReady();
        await this.pool.execute(`DELETE FROM ${this.t('message_secrets_cache')} WHERE session_id = ?`, [this.sessionId]);
    }
}
exports.WaMessageSecretMysqlStore = WaMessageSecretMysqlStore;
