import { type Pool, type PoolOptions } from 'mysql2/promise';
import type { WaMysqlMigrationDomain } from './types';
export declare function createMysqlPool(options: PoolOptions): Pool;
export declare function ensureMysqlMigrations(pool: Pool, domains: readonly WaMysqlMigrationDomain[], tablePrefix?: string): Promise<void>;
